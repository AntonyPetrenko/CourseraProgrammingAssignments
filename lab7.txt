 string textCenter = "****************************";
            Console.SetCursorPosition((Console.WindowWidth - textCenter.Length) / 2, Console.CursorTop);
            Console.WriteLine(textCenter);
            Console.SetCursorPosition((Console.WindowWidth - textCenter.Length) / 2, Console.CursorTop);
            Console.Write("Menu:");
            Console.WriteLine();
            Console.SetCursorPosition((Console.WindowWidth - textCenter.Length) / 2, Console.CursorTop);
            Console.WriteLine("1. - New Game");
            Console.SetCursorPosition((Console.WindowWidth - textCenter.Length) / 2, Console.CursorTop);
            Console.WriteLine("2. - Load Game ");
            Console.SetCursorPosition((Console.WindowWidth - textCenter.Length) / 2, Console.CursorTop);
            Console.WriteLine("3. - Options");
            Console.SetCursorPosition((Console.WindowWidth - textCenter.Length) / 2, Console.CursorTop);
            Console.WriteLine("4. - Quit");
            Console.SetCursorPosition((Console.WindowWidth - textCenter.Length) / 2, Console.CursorTop);
            Console.WriteLine(textCenter);
            Console.WriteLine();
            Console.Write("Enter a number from the menu ");
            var ans = Console.ReadLine();
            int choice = 0;
            if (int.TryParse(ans, out choice))
            {
                switch (choice)
                {
                    case 1:
                        //blah
                    case 2:
                        //gggg
                    case 3:
                        //jjjj
                    default:
            Console.WriteLine("Wrong Selection!" +
            Environment.NewLine + "Press any Key");
                        Console.ReadKey();
                        break;
                }
            }
        }
    }
}
